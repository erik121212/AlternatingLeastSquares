Start the Virtual box image


To launch Spark run the following in a terminal window: dse spark

Copy the data file "test.data" to /tmp

On the Spark console run the code from the file "als.scala"